<footer class="site-foot">
  <div class="site-foot__logo">
    <a href="/">
      <svg
        width="280"
        height="280"
        viewBox="0 0 280 280"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
      >
        <!-- Left Bracket `<` -->
        <path
          d="M90 60 L30 140 L90 220"
          stroke="currentColor"
          stroke-width="24"
          stroke-linecap="round"
          stroke-linejoin="round"
        />
        <!-- Slash `/` -->
        <path
          d="M120 240 L160 40"
          stroke="currentColor"
          stroke-width="24"
          stroke-linecap="round"
        />
        <!-- Right Bracket `>` -->
        <path
          d="M190 60 L250 140 L190 220"
          stroke="currentColor"
          stroke-width="24"
          stroke-linecap="round"
          stroke-linejoin="round"
        />
      </svg>

      <span class="u-sr-only">Anugrah K S</span>
    </a>
  </div><!-- .site-foot__logo -->
</footer>

<style lang="scss">
  .site-foot {
    @include flex(row, center, center);

    position: relative;
    z-index: 3;

    padding: 1.5rem;

    border-top: 1px solid color(secondary);

    @include mq(phone) {
      padding: 1rem;
    }

    &__logo {
      svg {
        display: block;
        width: 3rem;
        height: 3rem;

        path {
          stroke: color(secondary);
        }

        @include mq(phone) {
          width: 1.875rem;
          height: 1.875rem;
        }
      }
    }
  }
</style>
